
You must include as follows:

1. Perl file
 	use File::Basename;
	$MAIN_DIR=dirname($0);
	require ($MAIN_DIR."/conf/gconf.pl");

2. Python file
	import sys
	import os
	if os.path.dirname(sys.argv[0]):
        	sys.path.append(os.path.dirname(sys.argv[0]) + "/conf")
	else:
        	sys.path.append("./conf")
	from gconf import *


3. bash file
	MAIN_DIR=`dirname $0`
	source "$MAIN_DIR/conf/gconf.sh"

