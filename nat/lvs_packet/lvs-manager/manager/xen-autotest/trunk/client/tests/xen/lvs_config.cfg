lvs_path = /home/chenzhenchang/lvs/lvs_svn/online-conf/ccc.lvs_cluster.share_1

## service config 
service_name = hd.360.cn

## virtual service config 
vip = 123.125.74.67
vport = 80
protocol = TCP

## vrrp instance config
vrrp_name = vrrp
vrrp_instance = VI_101
virtual_router_id = 102
state = MASTER
priority = 110
interface = eth2

## real server config 
rip = 10.102.64.41:10.102.64.42:10.102.64.43
rport = 80
checker = HTTP_GET
url_path = /status.html
url_digest = eff5bc1ef8ec9d03e640fc4370f5eacd
connect_timeout = 5
nb_get_retry = 3
delay_before_retry = 3
virtualhost = hd.360.cn

## static ip 
static_ip = 10.1.1.120
bip = 192.168.83.249
dev = eth1
mask = 22

process = all

variants:
    - add_vrrp_sv:
	type = add_vrrp_sv
    - add_vrrp:
	type = add_vrrp
    - add_sv:
	type = add_sv
    - add_vs:
	type = add_vs
    - add_rs:
	type = add_rs
    - add_bip:
	type = add_bip
    - del_vrrp_sv:
	type = del_vrrp_sv
    - del_sv:
	type = del_sv
    - del_rs:
	type = del_rs
    - del_vrrp:
	type = del_vrrp
    - del_vs:
	type = del_vs
    - del_bip:
	type = del_bip
    - edit_vrrp:
	type = edit_vrrp
    - edit_vs:
	type = edit_vs
    - add_static_ip:
	type = add_static_ip
    - del_static_ip:
	type = del_static_ip
    - get_static_ip:
	type = get_static_ip

    - remote_backup_config:
	type = remote_backup_config
    - backup_config:
	type = backup_config
    - download_config:
	type = download_config
    - upload_config:
	type = upload_config
    - upload_alarm_conifg:
	type = upload_alarm_config
    - alarm_stop:
	typle = alarm_stop
    - reload_keepalived:
	type = reload_keepalived
    - alarm_start:
	typle = alarm_start
    - enable_vip:
	type = enable_vip
    - disable_vip:
	type = disable_vip
    - get_conf_all_sv:
	type = get_conf_all_sv
    - get_conf_sv:
	type = get_conf_sv
    - get_conf_vs:
	type = get_conf_vs
    - get_conf_rs:
	type = get_conf_rs
    - get_conf_bip:
	type = get_conf_bip
    - get_bvs_vs:
	type = get_bvs_vs
    - get_bvs_rs:
	type = get_bvs_rs
    - get_bvs_bip:
	type = get_bvs_bip






variants:
    add_service:
	only add_vrrp_sv add_vrrp add_sv add_vs add_rs add_bip
    
    add_service_without_vrrp_sv:
	only add_vrrp add_sv add_vs add_rs add_bip

    add_service_without_vip:
	only add_sv add_vs add_rs add_bip

    add_cluster_service:
	only add_sv add_vs add_rs add_bip
    
    add_rs_service:
	only add_rs

    add_vrrp_instance:
	only add_vrrp

    del_service: 
	only del_vrrp del_vs

    del_service_without_vrrp
	only del_vs

    del_cluster_service:
	only del_vs

    del_rs_service:
	only del_rs

    edit_service:
	only edit_vrrp edit_vs

    edit_cluster_service:
	only edit_vs

    edit_vrrp_service:
	only edit_vrrp

    test_case:
	only add_bip

    config_online:
	only remote_backup_config upload_config reload_keepalived enable_vip

only config_online
